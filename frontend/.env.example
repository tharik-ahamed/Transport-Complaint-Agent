# Frontend environment — Warning 2 fix
# Override these values to match the backend .env credentials.
# NEVER commit real credentials. These are dev defaults only.
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=admin123
